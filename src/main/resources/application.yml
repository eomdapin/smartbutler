# mysql 연동
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/smartbutler?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1111
  thymeleaf:
    cache: false
  
  #JPA 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    properties:
      hibernate:
#        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: update
    